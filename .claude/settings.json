{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // .tool_response.filePath // empty'); case \"$f\" in *.rs) cd \"$CLAUDE_PROJECT_DIR\" && cargo fmt -- \"$f\" || true ;; esac",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
